{
  "name": "brisol-teste-final",
  "version": "1.0.0",
  "description": "Brisol teste final - APK via GitHub Actions + Capacitor",
  "private": true,
  "scripts": {
    "build": "echo Web assets in www/",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
